import { sizeToString } from '../../../helpers/size-to-string';
import { module, test } from 'qunit';

module('Unit | Helper | size to string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sizeToString(42);
  assert.ok(result);
});
